<h1>Algorithm Wizard - Step 1/4</h1>
<%= progress_bar(1) %>

<h2>Information</h2>

<p>Thanks for your interest in contributing to the DIVAService. This wizard will guide you throuh the process of uploading your own Algorithm. First we need some general information about the algorithm and the author(s):</p>

<%= simple_form_for(@algorithm, url: wizard_path, method: :put, html: { class: 'form-horizontal' }) do |f| %>

  <h3>General information</h3>
  <%= f.input :name %>
  <%= f.input :namespace %>
  <%= f.input :description, as: :text %>

  <h3>Additional information</h3>
  <%= f.simple_fields_for :fields do |ff| %>
    <%= render partial: 'field', locals: { f: ff } %>
  <% end %>

  <%= link_to 'Cancel', algorithms_path, class: 'btn btn-default' %>
  <%= f.submit 'Next', class: 'btn btn-primary' %>
<% end %>
