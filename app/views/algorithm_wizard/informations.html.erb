<h1>Algorithm Wizard - Step 1/5</h1>
<%= progress_bar(1) %>

<h2>Information</h2>

<p>Thanks for your interest in contributing to the DIVAService. This wizard will guide you throuh the process of uploading your own Algorithm. First we need some general information about the algorithm and its author(s):</p>

<%= simple_form_for(@algorithm, url: wizard_path, method: :put) do |f| %>

  <h3>General information</h3>
  <%= f.simple_fields_for :general_fields do |ff| %>
    <%= render partial: 'field', locals: { f: ff } %>
  <% end %>

  <%= link_to 'Cancel', algorithms_path, class: 'btn btn-default' %>
  <%= f.submit 'Next', class: 'btn btn-primary' %>
<% end %>
